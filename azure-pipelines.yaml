---
pool:
  name: 'Azure Pipelines'
  vmImage: ubuntu-latest

trigger:
  - master

extends:
  template: ./templates/demo-parameters.yaml
  parameters:
    branch: 'mster'
    agentvmImage: 'ubuntu-18.04'
    jdkversion: '1.8'